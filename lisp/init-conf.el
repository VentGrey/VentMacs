;;; init-conf.el --- Configuraciones de C, el lenguaje de programación de los espartanos
;; -*- lexical-binding: t -*-

;;; Commentary:
;;
;; Configuraciones de archivos de configuración para Emacs.

;;; Code:

(use-package yaml-mode
  :defer t)
(use-package nginx-mode
  :defer t)
(use-package apache-mode
  :defer t)

(provide 'init-conf)
;;; init-conf.el ends here
